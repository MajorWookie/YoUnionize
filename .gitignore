node_modules
.env
.env.remote
.env.local
.env.*.local
dist
.zero
.drizzle
.sst
.turbo
.vite
*.tsbuildinfo
.DS_Store

# Lockfiles from non-Bun package managers (project uses bun.lock exclusively)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Section-prompt eval reports (per-run markdown outputs from
# scripts/eval-section-prompt.ts). Contain real filing snippets and
# Claude responses; meant for local review, not for committing.
eval-outputs/
