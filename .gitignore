# Dependencies
node_modules

# Environment files (never commit real secrets)
.env
.env.remote
.env.local
.env.*.local

# Build output
dist
*.tsbuildinfo

# Tooling caches
.zero
.drizzle
.sst
.turbo
.vite
.tamagui

# OS
.DS_Store

# Editor / local tooling state
.vscode/*.local
.idea
*.swp

# Claude Code agent worktrees and session state (never commit — duplicates the repo)
.claude/worktrees/
.claude/projects/
.claude/sessions/
.claude/settings.local.json

# Continue.dev local state
.continue/.session

# Supabase CLI scratch (project ref, pooler URL — regenerated by `supabase link`)
supabase/.temp/
supabase/.branches/

# Expo / native build artifacts
.expo/xcodebuild.log
.expo/xcodebuild-error.log
.expo/devices.json
.expo/web/cache/
ios/Pods/
ios/build/
android/.gradle/
android/build/
android/app/build/
android/local.properties
*.keystore.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
