# Local Supabase (default for `supabase start`)
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_KEY=

# Set to "false" to enable real auth in development.
# Defaults to "true" (auth bypassed) when unset.
VITE_DEV_SKIP_AUTH=true
