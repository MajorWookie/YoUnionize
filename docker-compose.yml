# Local development infrastructure
#
# PostgreSQL and storage are now provided by Supabase local (`supabase start`).
# This file is kept for any future non-Supabase services.
#
# Supabase local defaults:
#   Postgres:  postgresql://postgres:postgres@127.0.0.1:54322/postgres
#   API:       http://127.0.0.1:54321
#   Studio:    http://127.0.0.1:54323
#   Storage:   http://127.0.0.1:54321/storage/v1
#
# Run `supabase start` before `bun dev` for local development.
